import createRenderer from '../../helper';
import Answer from './';

describe('<Answer />', () => {
  xit('renders a clickable answer when poll is unanswered');
  xit('renders results when poll is answered');
  xit('calls castVote when clicked');
});
